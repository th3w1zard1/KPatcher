# Embedded integration mod trees (DEPRECATED)

This directory is scheduled for removal. All test data must be defined and constructed ephemerally in `.cs` files — in memory at test time, with zero committed fixture files on disk. See [docs/TESTING.md](../../../docs/TESTING.md) for the current policy.
